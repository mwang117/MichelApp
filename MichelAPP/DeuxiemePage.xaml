<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MichelAPP.DeuxiemePage"
             Title="Cafés"
             xmlns:local="clr-namespace:MichelAPP.ViewModels">

    <ContentPage.BindingContext>
        <local:CoffeeViewModel />
    </ContentPage.BindingContext>

    <StackLayout Padding="10">

        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />

        <CollectionView ItemsSource="{Binding Coffees}" SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10" Margin="5" BackgroundColor="LightGray">
                        <StackLayout>
                            <Image Source="{Binding Image}" HeightRequest="100" Aspect="AspectFit"/>
                            <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                            <Label Text="{Binding Description}" FontSize="14"/>
                            <Button Text="Voir les détails" CommandParameter="{Binding}" Clicked="OnItemSelected"/>
                        </StackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>

</ContentPage>
